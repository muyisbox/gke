name: vpa
chart: vpa
repoURL: https://charts.fairwinds.com/stable
targetRevision: "5.1.0"
namespace: vpa
cluster_env: gitops
helm:
  values:
    recommender:
      replicaCount: 2
    updater:
      replicaCount: 2
      resources:
        limits: {}
        requests:
          cpu: 50m
          memory: 500Mi
    podLabels:
      sidecar.istio.io/inject: "false"
