---
# Reloader values
reloader:
  watchGlobally: true
  readOnlyRootFilesystem: true

  deployment:
    resources:
      requests:
        cpu: 10m
        memory: 64Mi
      limits:
        memory: 128Mi

  serviceMonitor:
    enabled: true
    labels:
      release: prometheus-monitoring
